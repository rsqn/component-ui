ns("tech.rsqn.ui");


//todo - perhaps rename to control?
tech.rsqn.ui.Component = function () {
    //this.model = new tech.rsqn.core.Model();
};

tech.rsqn.ui.Component.isComponent = true;

extend(tech.rsqn.ui.Component, tech.rsqn.core.UIComponent);
